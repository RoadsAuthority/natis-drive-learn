PORT=3001
FRONTEND_ORIGIN=http://localhost:8080
# Quote the URL if it contains & (e.g. Neon channel_binding)
DATABASE_URL=""
JWT_SECRET=

# Comma-separated emails that register as verifier/admin (role = admin)
ADMIN_EMAILS=

